<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class=" w-full min-h-screen bg-zinc-900">
            <form action="/login" method="post" class="absolute left-1/2 top-1/2 -translate-x-[50%] -translate-y-[50%] flex flex-col gap-3 items-center justify-center text-white font-semibold">
                   <h3 class="text-white text-2xl font-semibold">Login to your Account</h3>
                   <input class="px-10 py-2 outline-none bg-transparent border-2 border-blue-600 rounded-[10px]" type="email" placeholder="email" name="email">
                    <input class="px-10 py-2 outline-none bg-transparent border-2 border-blue-600 rounded-[10px]" type="password" placeholder="password" name="password">
                    <input class="px-3 py-2 outline-none bg-blue-600 border-2 border-blue-600 rounded-[10px]" type="submit" value="login">
                    <span class="text-white text-xl font-semibold"> Not registered ?<a class="text-blue-500 text-xl font-semibold" href="/">Register Now</a></span>
            </form>
       
    </div>
</body>
</html>